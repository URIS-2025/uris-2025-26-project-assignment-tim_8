namespace SuggestionService.Clients
{
    // Minimal projection of SuggestionBoxService's SuggestionBoxDTO — only the field we need.
    public class SuggestionBoxVO
    {
        public Guid OrganizationId { get; set; }
    }
}
