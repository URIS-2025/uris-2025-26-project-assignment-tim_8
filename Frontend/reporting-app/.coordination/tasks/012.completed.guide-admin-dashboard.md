# Task 012: Guide — admin dashboard flow

**Type:** research
**Dependencies:** 001

## Goal
Write `.claude/guides/admin-dashboard.md` — how the protected `/admin/*` area is structured:
layout, navigation, and how pages render data through `DataTable`/`Modal`.

## Acceptance criteria
- [ ] Lists every file actually read.
- [ ] Covers: entry points, layout/nav, data flow into DataTable, modal CRUD pattern, extension points, gotchas, file map.
- [ ] Uses real names: `AdminLayout`, `Sidebar`, `AdminDashboard`, `OrganizationDetails`, `BoxDetails`, `UserManagement`.
- [ ] Repo-relative paths.

## Context
Read first: `.coordination/SCAN_REPORT.md`.
Read: `src/components/Layout/AdminLayout.jsx`, `src/components/Layout/Sidebar.jsx`,
`src/pages/AdminDashboard.jsx`, `src/pages/OrganizationDetails.jsx`, `src/pages/UserManagement.jsx`,
`src/pages/BoxDetails.jsx`, `src/components/DataTable.jsx`, `src/components/Modal.jsx`.

Flow to document:
- `/admin` route → `<ProtectedRoute><AdminLayout/></ProtectedRoute>` with `<Sidebar/>` + `<Outlet/>`.
- Sub-routes (dashboard, organizations/:orgId, boxes/:boxId, submissions/:submissionId, billing,
  community, suggestions, problems, users [admin-only], settings).
- Page pattern: `useEffect` fetch via a service → `useState` data/loading/error → feed `data` + a
  `columns` config to `DataTable` → row/action click opens `Modal` for detail/edit/create.
- Search/pagination is controlled state passed into `DataTable` (`searchValue`, `currentPage`, `onPageChange`).

Gotchas: `/admin/organizations` redirects to a hardcoded `ORG-001`; `users` page is double-guarded
(`allowedRoles={['admin']}`). Cross-reference rules 003 (data-fetching), 004 (routing), 006 (components).

## Findings
<!-- Worker fills in -->

## Solution
Wrote `.claude/guides/admin-dashboard.md`. `/admin` ProtectedRoute+AdminLayout(Sidebar+Outlet), sub-route table, fetch->useState->DataTable->Modal data flow, role-filtered sidebar (`Sidebar.jsx:24-31`); gotchas (ORG-001 hardcoded redirect, `/admin/users` double-guard, Billing nav vs route gating mismatch).
