# Task 014: Index — write CLAUDE.md

**Type:** code
**Dependencies:** 002, 003, 004, 005, 006, 007, 008, 009, 010, 011, 012, 013

## Goal
Write the frontend-scoped `CLAUDE.md` at `Frontend/reporting-app/CLAUDE.md` indexing every rule
and guide produced by tasks 002–013.

## Acceptance criteria
- [ ] 2–3 sentence project description (frontend SPA).
- [ ] Build / test / dev server commands as fenced code blocks.
- [ ] Architecture table: layer | technology | purpose.
- [ ] Rules table: one row per `.claude/rules/*.md`, one-line scope.
- [ ] Guides table: one row per `.claude/guides/*.md`, one-line topic.
- [ ] Constraints section (hard rules specific to this codebase).
- [ ] Does NOT overwrite the repo-root `CLAUDE.md` (backend-oriented). This file is in the app dir.
- [ ] Repo-relative paths; tables over prose.

## Context
Read first: `.coordination/SCAN_REPORT.md`, then every file produced under `.claude/rules/` and
`.claude/guides/` (confirm actual filenames before tabulating — do not assume; `ls .claude/rules .claude/guides`).

Expected rule files (from tasks 002–008): services-api, data-fetching-state, routing-guards,
auth-context, shared-components, styling, forms-validation.
Expected guide files (from tasks 009–013): architecture-overview, auth-flow, api-contract-mapping,
admin-dashboard, anonymous-flow.

Commands (from package.json): `npm install`, `npm start` (:3000), `npm test`, `npm run build`.

Architecture table rows to include: Routing (react-router-dom 7), State (useState + AuthContext),
Data (services/*.js + fetch), Auth (jwt-decode + localStorage), UI (lucide-react + plain CSS),
Gateway (Nginx :80, `/api/*`).

Constraints to state (verified facts): API base hardcoded `http://127.0.0.1:80` in every service +
AuthContext (no env/proxy); requires Docker gateway running for API work; tokens in localStorage;
no global store / no form lib / no CSS framework; roles are lowercase strings.

## Findings
<!-- Worker fills in -->
