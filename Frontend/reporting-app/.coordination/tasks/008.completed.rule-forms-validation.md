# Task 008: Rule — forms & client-side validation

**Type:** code
**Dependencies:** 001

## Goal
Write `.claude/rules/forms-validation.md` covering the controlled-input + manual-validation
pattern (no form library) and how submit errors are surfaced.

## Acceptance criteria
- [ ] File ≤500 lines, table-heavy, WRONG/CORRECT pairs.
- [ ] ≥3 real `file:line` occurrences cited.
- [ ] Uses real names: `AnonymousSignup.jsx`, `Login.jsx`, `AnonymousSubmit.jsx`; real validation lines (e.g. "Password must be at least 8 characters.").
- [ ] Documents: controlled inputs via `useState`, validate-then-submit, `setError('')` reset, error message rendered inline.

## Context
Read first: `.coordination/SCAN_REPORT.md`.
Read: `src/pages/AnonymousSignup.jsx` (has explicit validation at lines ~27–52: clears error,
checks empty fields, password length ≥8, password match, catch → `setError(err.message || ...)`),
`src/pages/Login.jsx`, `src/pages/AnonymousSubmit.jsx`.

Confirmed (planner scan):
- No Formik/react-hook-form/yup. Inputs are controlled (`value` + `onChange` → `useState`).
- Validation is hand-written inside the submit handler: reset `setError('')`, guard clauses with
  early `return` + `setError('...')`, then call the service, then `catch` sets error from `err.message`.
- Errors render inline (an error `<div>`/`<p>`), not via toast/alert.

Rule content (WRONG/CORRECT):
- Controlled inputs bound to `useState`, no uncontrolled refs.
- Validate in the submit handler with guard clauses before calling the service.
- Clear previous error (`setError('')`) at the start of submit.
- Surface server errors via `catch` → `setError(err.message)`; the service already threw a message.

Evidence greps:
- `grep -rn "onChange={" src/pages | head`
- `grep -rn "setError('')\|setError('" src/pages/AnonymousSignup.jsx src/pages/Login.jsx`
- `grep -rln "onSubmit\|handleSubmit" src/pages`

## Findings
<!-- Worker fills in -->

## Solution
Wrote `.claude/rules/forms-validation.md`. Native `<form onSubmit>` + manual handleSubmit contract (preventDefault -> setError('') -> guard clauses -> setIsSubmitting -> service -> catch setError(err.message) -> finally), controlled + object-form + e.target.name.value input styles, inline AlertCircle error block; flags `AnonymousLogin.jsx` `alert()` as the outlier. Evidence: 5 onSubmit forms, 67 onChange.
