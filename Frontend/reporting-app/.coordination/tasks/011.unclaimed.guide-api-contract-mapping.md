# Task 011: Guide — API contract mapping (frontend ↔ backend)

**Type:** research
**Dependencies:** 001

## Goal
Write `.claude/guides/api-contract-mapping.md` — how the 21 `src/services/*.js` modules map to
the 11 backend services through the Nginx gateway, and the request/response DTO shapes consumed.

## Acceptance criteria
- [ ] Lists every file actually read.
- [ ] Table mapping: service module → `/api/*` route(s) → backend service (per repo-root CLAUDE.md table).
- [ ] Documents DTO shapes the frontend depends on (e.g. login returns `{accessToken, refreshToken}`; UserRole has `.title`).
- [ ] Notes error contract: `data.errors` / `data.error` / `data.title` (see `extractErrorMessage`).
- [ ] Repo-relative paths.

## Context
Read first: `.coordination/SCAN_REPORT.md` and repo-root `CLAUDE.md` (backend service table + gateway notes).
Read: ALL 21 files in `src/services/` (they're short), and `gateway/nginx.conf` if present at repo root
to confirm route prefixes.

The 21 service modules:
anonymousUserService, attachmentService, billingNotificationService, boxAccessLinkService,
organizationService, paymentService, problemBoxService, problemCategoryService, problemCommentService,
problemService, subscriptionPlanService, subscriptionService, suggestionBoxService,
suggestionCategoryService, suggestionCommentService, suggestionService, systemNotificationService,
systemUserService, userRoleService, userService, voteService.

Backend services (repo-root CLAUDE.md): AnonymousUserService, OrganizationService, ProblemService,
ProblemBoxService, SuggestionService, SuggestionBoxService, SubscriptionService,
BillingNotificationService, SystemNotificationService, AttachmentService, LoggerService.

Build the mapping table from the `${API_BASE_URL}/api/<Resource>/...` paths inside each module.
Note: there are NO shared TypeScript types — the contract is implicit in each `fetch` URL + the
JSON shape the page consumes. Call that out as a gotcha.

DTO facts already known: login/refresh → `{accessToken, refreshToken}`; `UserRoleService.getById`
→ object with `.title`; error bodies parsed as `{errors|error|title}`.

## Findings
<!-- Worker fills in -->
