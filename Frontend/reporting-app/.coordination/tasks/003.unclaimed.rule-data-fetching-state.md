# Task 003: Rule — data fetching & component state

**Type:** code
**Dependencies:** 001

## Goal
Write `.claude/rules/data-fetching-state.md` covering how pages load data: `useState` for
`data`/`loading`/`error`, fetch inside `useEffect`, and how loading/error are rendered.

## Acceptance criteria
- [ ] File ≤500 lines, table-heavy, WRONG/CORRECT pairs.
- [ ] ≥3 real `file:line` occurrences cited per documented pattern.
- [ ] Uses real page names: `BoxDetails.jsx`, `OrganizationDetails.jsx`, `SubmissionDetails.jsx`.
- [ ] Documents: `useState(true)` initial loading, `setError(null)`, try/catch/finally, early-return loading/error UI.

## Context
Read first: `.coordination/SCAN_REPORT.md`.
Read: `src/pages/BoxDetails.jsx` (has `const [loading, setLoading] = useState(true)` and
`const [error, setError] = useState(null)` at lines ~43–44), `src/pages/OrganizationDetails.jsx`,
`src/pages/SubmissionDetails.jsx`, `src/pages/AdminDashboard.jsx`.

Confirmed (planner scan): 13 pages use `useEffect`; 18 use `useState`. Loading/error state is
local per page (no React Query, no SWR, no global store). Service calls are awaited inside an
async function defined in `useEffect`.

Rule content (WRONG/CORRECT):
- Fetch in `useEffect`, not in render body.
- Initialize `loading` true, set false in `finally`.
- Always catch and `setError(err.message)`; render error/loading branches before main UI.
- Don't store server data in context — keep it local `useState`.

Evidence greps:
- `grep -rln "useEffect" src/pages`
- `grep -rn "useState(true)\|setLoading\|setError" src/pages`
- `grep -rn "} finally {" src/pages`

## Findings
<!-- Worker fills in -->
