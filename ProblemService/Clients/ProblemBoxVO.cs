namespace ProblemService.Clients
{
    // Minimal projection of ProblemBoxService's ProblemBoxDTO — only the field we need.
    public class ProblemBoxVO
    {
        public Guid OrganizationId { get; set; }
    }
}
