# Task 002: rule-controllers-and-errors

**Type:** code
**Dependencies:** 001

## Goal
Write `.claude/rules/controllers-and-errors.md` — how to write a controller in this repo:
attributes, action signatures, the try/catch + `TryLogAsync` audit pattern, and the
`new { error = ... }` response shape. WRONG/CORRECT pairs from real controllers.

## Acceptance criteria
- [ ] File `.claude/rules/controllers-and-errors.md` created, <500 lines, table/WRONG-CORRECT heavy.
- [ ] Findings section lists 3+ real `file:line` occurrences per claimed pattern.
- [ ] Uses real names (`ProblemController.CreateProblem`, `LogCreationDTO`, etc.).
- [ ] Covers: `[ApiController]` + `[Route("api/[controller]")]`; `ActionResult<T>` returns;
      `Created("", result)` / `Ok` / `NoContent` / `NotFound` / `BadRequest`; the audit-log
      call on BOTH success and failure paths; the `new { error = ex.Message }` shape;
      which exception types are caught (`KeyNotFoundException`, `ArgumentException`).
- [ ] Documents the `error` (controller) vs `message` (global handler) key inconsistency as
      a known gotcha — do not "fix" it, just state it.

## Findings (grep BEFORE writing — record results here)
Start from these, confirm with your own greps:
- `TryLogAsync` → 106 occurrences / 18 controllers (`grep -rn "TryLogAsync" --include=*.cs Controllers`).
- Error shape `return BadRequest(new { error` / `return NotFound(new { error` → 52 / 19.
- Canonical example: `ProblemService/Controllers/ProblemController.cs:46-80` (CreateProblem:
  try → repo call → success log → `Created`; catch → failure log → `BadRequest(new { error })`).
- `[ApiController]` + `[Route("api/[controller]")]` on every controller class.

## Context
- Reference controller: `ProblemService/Controllers/ProblemController.cs` (full file, read it).
- Compare a second + third: `OrganizationService/Controllers/UserController.cs`,
  `SuggestionService/Controllers/SuggestionController.cs`.
- The audit-log DTO shape is in `LoggerService/Models/DTOs/LogCreationDTO.cs` — fields used at
  call sites: `UserId = User.Identity?.Name`, `Action`, `EntityName`, `NewValues`/`OldValues`
  (JsonSerializer.Serialize), `IsSuccess`, `ServiceName`, `HttpMethod`, plus
  `Request.Headers["Authorization"]` and `HttpContext.RequestAborted`.
- Global handler: `ProblemService/Program.cs:70-85` (`UseExceptionHandler` → `{ message }`).

## Findings

### [ApiController] + [Route("api/[controller]")] — verified on every controller
- `ProblemService/Controllers/ProblemController.cs:10-11`
- `OrganizationService/Controllers/UserController.cs:13-14`
- `SuggestionService/Controllers/SuggestionController.cs:10-11`
- `AnonymousUserService/Controllers/AnonymousUserController.cs:13-14`
- `BillingNotificationService/Controllers/BillingNotificationController.cs:12-13`

### TryLogAsync — 106 occurrences / 18 controllers (grep confirmed)
- `ProblemService/Controllers/ProblemController.cs:53` (success path, CreateProblem)
- `ProblemService/Controllers/ProblemController.cs:68` (failure path, CreateProblem)
- `OrganizationService/Controllers/UserController.cs:50` (success path, CreateUser)
- `OrganizationService/Controllers/UserController.cs:69` (failure path, CreateUser)
- `SuggestionService/Controllers/SuggestionController.cs:50` (success path, CreateSuggestion)
- `SuggestionService/Controllers/SuggestionController.cs:65` (failure path, CreateSuggestion)

### return BadRequest(new { error = ex.Message }) — 20+ occurrences across 19 controllers
- `ProblemService/Controllers/ProblemController.cs:78`
- `OrganizationService/Controllers/UserController.cs:79`
- `SuggestionService/Controllers/SuggestionController.cs:75`
- `ProblemService/Controllers/ProblemCategoryController.cs:71`
- `BillingNotificationService/Controllers/BillingNotificationController.cs:73`

### return NotFound(new { error = ex.Message }) — verified
- `ProblemService/Controllers/ProblemController.cs:114` (UpdateProblem, KeyNotFoundException)
- `ProblemService/Controllers/ProblemController.cs:151` (DeleteProblem, KeyNotFoundException)
- `OrganizationService/Controllers/UserController.cs:118` (UpdateUser, KeyNotFoundException)
- `OrganizationService/Controllers/UserController.cs:156` (DeleteUser, KeyNotFoundException)
- `SuggestionService/Controllers/SuggestionController.cs:111` (UpdateSuggestion, KeyNotFoundException)
- `AttachmentService/Controllers/AttachmentController.cs:115`

### return Created("", result) — verified across services
- `ProblemService/Controllers/ProblemController.cs:64`
- `OrganizationService/Controllers/UserController.cs:61`
- `SuggestionService/Controllers/SuggestionController.cs:61`
- `AnonymousUserService/Controllers/AnonymousUserController.cs:51`
- `AttachmentService/Controllers/AttachmentController.cs:65`

### LogCreationDTO field set — verified
- `LoggerService/Models/DTOs/LogCreationDTO.cs:7-14` (canonical definition: UserId, Action, EntityName, OldValues, NewValues, IsSuccess, ServiceName, HttpMethod)
- `ProblemService/Controllers/ProblemController.cs:53-62` (all 7 fields set on success path)
- `OrganizationService/Controllers/UserController.cs:93-102` (OldValues + NewValues both set for update)

### Global handler uses key "message", controllers use key "error"
- `ProblemService/Program.cs:70-85` (global handler: `new { message = error.Error.Message }`)
- `ProblemService/Controllers/ProblemController.cs:78` (controller: `new { error = ex.Message }`)
- `LoggerService/Controllers/LoggerController.cs:71` (third variant: `new { error, detail }`)

## Solution

Produced `.claude/rules/controllers-and-errors.md` (305 lines): a table-heavy, WRONG/CORRECT rule file documenting the `[ApiController]+[Route]` class attributes, constructor injection of `I<X>Repository`/`IMapper`/`LoggerServiceClient`, `ActionResult<T>` signatures, the full `try/catch+TryLogAsync` audit-log pattern with both success and failure paths, all result helpers (`Created`/`Ok`/`NoContent`/`NotFound`/`BadRequest`), exception-type routing, and the known `error` vs `message` key inconsistency between controller catches and the global `UseExceptionHandler`.
