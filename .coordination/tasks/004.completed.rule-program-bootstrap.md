# Task 004: rule-program-bootstrap

**Type:** code
**Dependencies:** 001

## Goal
Write `.claude/rules/program-bootstrap.md` — the exact wiring every service's `Program.cs`
must have, in order: DbContext + connection string, repositories scoped, AutoMapper,
controllers + Swagger, named HttpClients, Kestrel on 8080, `Database.Migrate()` at startup.
This is the checklist for adding a NEW service or service feature.

## Acceptance criteria
- [ ] File `.claude/rules/program-bootstrap.md` created, <500 lines.
- [ ] Findings lists 3+ real `file:line` per claim.
- [ ] Covers, with a CORRECT skeleton block:
      - `AddDbContext<XxxContext>(o => o.UseSqlServer(builder.Configuration.GetConnectionString("XxxDB")))`
      - service-specific connection-string KEY (not a generic "DefaultConnection")
      - `AddAutoMapper(config => config.AddMaps(typeof(Program).Assembly))`
      - `AddScoped<IRepo, Repo>()` for each repository
      - `AddControllers()` + `AddEndpointsApiExplorer()` + `AddSwaggerGen()`
      - `builder.WebHost.ConfigureKestrel(o => o.ListenAnyIP(8080))` (ALL services use 8080)
      - startup migrate block: `using (var scope = app.Services.CreateScope()) { ... Database.Migrate(); }`
        — note the `IsRelational()`/`EnsureCreated()` and `IsEnvironment("Testing")` guards
      - `public partial class Program { }` at file end (required for WebApplicationFactory tests)
- [ ] EF migration workflow as a short table: `dotnet ef migrations add <Name>`,
      `dotnet ef database update`; migrations live in `<Service>/Migrations/`.
- [ ] WRONG/CORRECT: forgetting `partial class Program` (breaks integration tests),
      hardcoding port, using `EnsureCreated` in production path.

## Findings (grep BEFORE writing — record results here)
- `ListenAnyIP(8080)` → `grep -rn "ListenAnyIP" --include=Program.cs` (expect all 10 services).
- `Database.Migrate()` → present in each `Program.cs`. Two variants:
  - `ProblemService/Program.cs:50-61` (IsRelational/EnsureCreated branch)
  - `AnonymousUserService/Program.cs:87-92` (`IsEnvironment("Testing")` guard)
- `AddAutoMapper(config => config.AddMaps(typeof(Program).Assembly))` → `ProblemService/Program.cs:16`.
- `public partial class Program { }` → `ProblemService/Program.cs:93`, `AnonymousUserService/Program.cs:111`.
- Connection key: `GetConnectionString("ProblemDB")` (`ProblemService/Program.cs:10`),
  `GetConnectionString("AnonymousUserDB")` (`AnonymousUserService/Program.cs:14`).

## Context
- Read: `ProblemService/Program.cs` (full — simplest canonical bootstrap).
- Read: `AnonymousUserService/Program.cs` (full — superset: adds auth + rate limiter; note
  ordering of `UseRateLimiter` → `UseAuthentication` → `UseAuthorization` → `MapControllers`).
- Read: `ProblemService/appsettings.json` (connection string + `Services` block shape).
- Do NOT document JWT/rate-limiter wiring here — that's task 006 (rule-auth-jwt). Cross-link it.

## Findings

### `ListenAnyIP(8080)` — all services pin port 8080
- `ProblemService/Program.cs:45`: `options.ListenAnyIP(8080);`
- `AnonymousUserService/Program.cs:66`: `options.ListenAnyIP(8080);`
- `OrganizationService/Program.cs:68`: `options.ListenAnyIP(8080);`
- `SuggestionBoxService/Program.cs:48`: `options.ListenAnyIP(8080);`
- `BillingNotificationService/Program.cs:28`: `options.ListenAnyIP(8080);`
- `SystemNotificationService/Program.cs:31`: `options.ListenAnyIP(8080);`
- `SubscriptionService/Program.cs:52`: `options.ListenAnyIP(8080);`
- `ProblemBoxService/Program.cs:38`: `options.ListenAnyIP(8080);`
Note: `AttachmentService`, `SuggestionService`, and `LoggerService` do NOT declare a Kestrel
listener in their `Program.cs` but still resolve to `:8080` via Docker's default port mapping.
All 8 services that do declare it use `8080`.

### `Database.Migrate()` — present in all 11 Program.cs files
- `ProblemService/Program.cs:55` — inside `if (db.Database.IsRelational())` branch (Variant A)
- `AttachmentService/Program.cs:29` — inside `if (db.Database.IsRelational())` (Variant A)
- `SuggestionBoxService/Program.cs:58` — inside `if (db.Database.IsRelational())` (Variant A)
- `AnonymousUserService/Program.cs:91` — guarded `if (!app.Environment.IsEnvironment("Testing"))` (Variant B)
- `OrganizationService/Program.cs:93` — guarded `if (!app.Environment.IsEnvironment("Testing"))` (Variant B)
- `SuggestionService/Program.cs:42` — guarded `if (!app.Environment.IsEnvironment("Testing"))` (Variant B)
- `LoggerService/Program.cs:30` — guarded via `env.IsEnvironment("Testing")` resolved from scope (Variant B)
- `BillingNotificationService/Program.cs:37`, `SystemNotificationService/Program.cs:40`,
  `SubscriptionService/Program.cs:65`, `ProblemBoxService/Program.cs:49` — Variant B

### `AddAutoMapper(config => config.AddMaps(typeof(Program).Assembly))` — all 11 services
- `ProblemService/Program.cs:16`
- `AnonymousUserService/Program.cs:18`
- `AttachmentService/Program.cs:14`
- `OrganizationService/Program.cs:17`
- `LoggerService/Program.cs:14`
- `BillingNotificationService/Program.cs:13`
- `SystemNotificationService/Program.cs:17`
- `SuggestionService/Program.cs:34`
- `SubscriptionService/Program.cs:24`
- `ProblemBoxService/Program.cs:17`
- `SuggestionBoxService/Program.cs:25` (uses shorter form `AddAutoMapper(typeof(Program).Assembly)`)

### `public partial class Program { }` — all 11 services
- `ProblemService/Program.cs:93`
- `AnonymousUserService/Program.cs:111`
- `OrganizationService/Program.cs:113`
- `LoggerService/Program.cs:70`
- `AttachmentService/Program.cs:43`
- `SuggestionService/Program.cs:55`
- `SuggestionBoxService/Program.cs:78`
- `BillingNotificationService/Program.cs:53`
- `SystemNotificationService/Program.cs:63`
- `SubscriptionService/Program.cs:83`
- `ProblemBoxService/Program.cs:87`

### Service-specific `GetConnectionString` keys
- `ProblemService/Program.cs:10`: `GetConnectionString("ProblemDB")`
- `AnonymousUserService/Program.cs:14`: `GetConnectionString("AnonymousUserDB")`
- `OrganizationService/Program.cs:14`: `GetConnectionString("OrganizationDb")`
- `AttachmentService/Program.cs:12`: `GetConnectionString("AttachmentDB")`
- `SuggestionService/Program.cs:15`: `GetConnectionString("SuggestionDB")`
- `SuggestionBoxService/Program.cs:22`: `GetConnectionString("SuggestionBoxDB")`
- `SubscriptionService/Program.cs:21`: `GetConnectionString("SubscriptionDB")`
- `BillingNotificationService/Program.cs:10`: `GetConnectionString("BillingNotificationDB")`
- `SystemNotificationService/Program.cs:11`: `GetConnectionString("SystemNotificationDB")`
- `LoggerService/Program.cs:20`: `GetConnectionString("LoggerDB")`
- `ProblemBoxService/Program.cs:13`: `GetConnectionString("ProblemBoxDB")`

## Solution

Rule file `.claude/rules/program-bootstrap.md` (321 lines) documents the complete, ordered
`Program.cs` wiring checklist for all 11 services, covering the DbContext+service-specific
connection-string key, scoped repos, AutoMapper, Kestrel port 8080, both startup-migrate guard
variants (IsRelational/EnsureCreated and IsEnvironment("Testing")), global exception handler,
`public partial class Program`, the EF migration workflow table, and a full WRONG/CORRECT table
and copy-paste skeleton for new services.
