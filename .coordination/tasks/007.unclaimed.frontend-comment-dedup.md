# Task 007: Frontend Comment-Notification Dedup (Agent E, part 2)

**Type:** code
**Dependencies:** 002, 003

## Goal
Now that ProblemService (task 002) and SuggestionService (task 003) produce comment system-notifications on the backend, remove the duplicate frontend-side `SystemNotificationService.create(...)` call in `SubmissionDetails.jsx` so comment notifications are produced exactly once. Confirm the org page still displays them.

## Acceptance criteria
- [ ] The frontend no longer creates a system notification when staff reply to a comment (the `// 2. Create SystemNotification` block is removed).
- [ ] The reply flow still works: comment is created, input cleared, comments refreshed.
- [ ] No now-unused imports left dangling (remove the `SystemNotificationService` import if nothing else in the file uses it).
- [ ] `OrganizationDetails.jsx` still displays system notifications for the right org (display path untouched, just verify — it fetches via `getAll()` and filters client-side by `organizationId`).
- [ ] `npm run build` succeeds (no unused-var/lint breakage from the removal).

## Context

- **Depends on 002 & 003 landing** — only remove the frontend create once the backend actually produces comment notifications, otherwise comment notifications disappear entirely. Confirm those tasks are `completed` before starting.
- File: `Frontend/reporting-app/src/pages/SubmissionDetails.jsx`. The block to remove is around lines 193–204:
  ```jsx
  // 2. Create SystemNotification
  try {
      await SystemNotificationService.create({
          text: replyText,
          suggestionCommentId: !isProblem ? (createdComment.id || null) : null,
          problemCommentId: isProblem ? (createdComment.id || null) : null,
          organizationId: suggestion?.organizationId || null,
          anonymousUserId: suggestion?.anonymousUserId || null
      });
  } catch (notifErr) {
      console.error('Failed to create system notification:', notifErr);
  }
  ```
  Keep the surrounding comment-create (step 1) and the refresh/clear (step 3) intact.
- Import at line 10: `import { SystemNotificationService } from '../services/systemNotificationService';` — remove it only if no other usage remains in the file (grep the file first).
- Display side to verify (do NOT change): `Frontend/reporting-app/src/pages/OrganizationDetails.jsx` fetches both notification streams via `getAll()` and filters by `organizationId`.

## Gotchas
- This is the one cross-task sequencing point in the plan — landing it before 002/003 would drop comment notifications. Verify dependencies are completed.
- Note the backend now produces the comment notification for ANY author (not just staff replies), so behavior is a superset of the old frontend-only path — that's intended.

## Branch
<!-- execute-task writes the feature branch name here. Branch off feature/auth-input-hardening. -->

## Findings / Solution
<!-- execute-task fills this in on completion. -->
