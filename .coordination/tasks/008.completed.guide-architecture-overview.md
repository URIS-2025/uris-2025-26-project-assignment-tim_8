# Task 008: guide-architecture-overview

**Type:** both
**Dependencies:** 001

## Goal
Write `.claude/guides/architecture-overview.md` — the 1-screen mental model: 11 services +
gateway + per-service SQL Server DBs + React SPA, how they're wired in Docker, ports, and the
deployment topology. This is what an agent reads first.

## Acceptance criteria
- [ ] File `.claude/guides/architecture-overview.md` created.
- [ ] Lists every file actually read (with repo-relative paths) in a "Sources" section.
- [ ] Architecture table: service | container name | DB name | exposed? | one-line purpose (all 11).
- [ ] Topology: frontend (:3000 dev) → Nginx gateway (:80) → services (:8080 internal) →
      SQL Server (sql-server:1433). Only gateway + sql-server published.
- [ ] Deployment: `docker-compose up -d`; how services resolve each other by container name;
      where connection strings + `Services` URLs + `Jwt:*` come from (appsettings + compose env).
- [ ] Gotchas section: no shared project (duplicated `AnonymousDomain` models); auth only in 2
      services; `Database.Migrate()` on startup; test-project naming is inconsistent.
- [ ] File map: top-level dirs and what each service folder contains
      (`Controllers/ Models/ Data/ Context/ Migrations/ Clients/ ServiceCalls/ Profiles/`).

## Context
- Read: `docker-compose.yml` (services, container names, ports, env, depends_on, volumes).
- Read: `gateway/nginx.conf` (upstreams + published port).
- Read: root `CLAUDE.md` (existing overview — extend, don't contradict; note it now documents
  the `:3000` dev server needing the `:80` gateway stack running).
- Read: one `appsettings.json` (`ProblemService/appsettings.json`) + one
  `appsettings.Development.json` for env differences; the `.csproj` of one service for TFM (net8.0).
- Read: one `Dockerfile` (`ProblemService/Dockerfile`) for the build/runtime image pattern.
- Cross-link rules 004 (bootstrap), 005 (inter-service), 007 (gateway).

## Solution

Created `.claude/guides/architecture-overview.md` covering all 11 services in a table (container name, DB name, exposed status, purpose), the full React → Nginx → services → SQL Server topology with ports, deployment configuration sources (appsettings vs compose env), the standard per-service folder layout, top-level repo structure, and six documented gotchas (no shared project, JWT in 2 services only, startup migrations, inconsistent test naming, two HTTP client styles, error-key mismatch), with cross-links to the three related rules files and a Sources section listing all 9 files read.
