# Task 012: index-claude-md

**Type:** code
**Dependencies:** 002, 003, 004, 005, 006, 007, 008, 009, 010, 011

## Goal
Rewrite the root `CLAUDE.md` so it indexes the new `.claude/rules/` and `.claude/guides/`
files and serves as the single entry point future sessions load. Extend the existing
`CLAUDE.md` (do not discard its accurate frontend + commands + testing detail).

## Acceptance criteria
- [ ] Root `CLAUDE.md` updated, containing:
      - 2–3 sentence project description.
      - Build / test / run command blocks (Docker, backend, frontend) — keep the existing
        accurate ones; verify against the current files.
      - Architecture table: service | language/framework | purpose | comms pattern.
      - **Rules table**: one row per file in `.claude/rules/` — filename | one-line scope.
      - **Guides table**: one row per file in `.claude/guides/` — filename | one-line topic.
      - **Constraints section**: hard rules specific to THIS codebase (e.g. all services on
        Kestrel 8080; `Database.Migrate()` on startup; auth only in AnonymousUserService +
        OrganizationService; never return entities — DTO via Profiles; every new controller
        needs an nginx `location` block; prefer `Clients/` over `ServiceCalls/`; duplicated
        `AnonymousDomain` models must be edited in all copies; `public partial class Program`
        required for integration tests).
- [ ] Tables over paragraphs. Repo-relative paths. Every line earns its place.
- [ ] Verify each referenced rule/guide file actually exists before linking it.

## Context
- Read every file under `.claude/rules/` and `.claude/guides/` produced by tasks 002–011, plus
  `.coordination/SCAN_REPORT.md`, to write accurate one-line scopes (don't guess).
- Read the current root `CLAUDE.md` (already accurate on commands, frontend, testing,
  connection strings) and the just-updated testing/frontend notes — preserve them.
- This is the LAST task; it depends on all rule + guide tasks being `completed`.
- After writing, the workflow is done — suggest the user run `/compile-results`.

## Solution

Rewrote root `CLAUDE.md` (orchestrator, not a fan-out agent — this task is serial and depends
on all 10 outputs). Preserved all existing accurate content (commands, frontend, connection
strings, testing-inconsistency note). Added: a **Knowledge Base** section near the top indexing
all 6 rules + 4 guides with one-line scopes (+ pointer to `.coordination/SCAN_REPORT.md`); a
**"Calls out to"** column on the architecture table plus the `Clients/` vs `ServiceCalls/` note;
and a **Constraints** section of 12 hard rules (Kestrel 8080, `partial class Program`, migrate-on-
startup, service-specific conn keys, never-return-entities, `new { error }` vs `{ message }`
mismatch, audit-on-both-paths, gateway location-per-controller, prefer Clients/, JWT only in 2
services, duplicated `AnonymousDomain` models, and the two known config bugs). Verified all 10
referenced rule/guide files exist before linking.
